# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository context

This is a personal OneDrive Documents folder, not a software project. It contains:

- Design/art university coursework (InDesign `.indd`, Blender `.blend` files under `Uade (ara)/`)
- Audio production files (FL Studio settings and vendor-provided Arturia MIDI controller scripts under `Image-Line/`)
- Game saves and configs for various titles
- Personal documents (`.docx`, `.xlsx`, `.txt`)

There is no application to build, no test suite, and no lint configuration. The Python files present are vendor-supplied FL Studio hardware scripts, not project code.

## If you're working on a specific task here

Clarify with the user which file or folder the task concerns, since there is no unified project structure.
